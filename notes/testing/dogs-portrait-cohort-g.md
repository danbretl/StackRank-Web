# Dogs portrait cohort G

## Current state — September 23, 2026

G01 is the final 25-pair milestone of Dan’s new 100-pair continuation, from verified baseline307
to407. The previous three milestones are live at382. G is not published until the production receipt
below is complete. The main app continues to hide unfinished entries without deleting catalog or saved data.

## Frozen selection and ownership

`data/dogs/portrait-cohort-g.json` preserves 25 ordered primaries and 15 ordered reserves, linked to
run ordinals76–100. G’s digest is `1edeac15cdf5bbc8734b975f69c6d7608916c96eed4d8a1b772cbfe9f7ea2726`;
the parent run remains `1817c6548fdc677d29731ad42956dac23eb1dcf20f7557612eea3c02a8258af8`.
E and F remain independently frozen. Selection amendments are append-only, never silent substitutions.

Three reused workers used GPT-6 Sol / High for research, writing, generation and first-pass review.
The primary independently reviewed each accepted native master, final full/short description and contact
sheet, owned shared integration, and publishes releases. Exact root model configuration and underlying
imagegen model are undisclosed. No external image API or CLI was used; costs/tokens are unavailable.

## Evidence and pipeline

Native originals and hashes stay in ignored `assets/dogs/generated-masters/cohort-g/`; packets, pinned
Commons revisions, original/reference-input hashes, primary source snapshots, exact prompts and actual
call receipts stay under `reports/dogs-generated-artwork/continuation-100/g01-*`. Tracked batch and
cohort records retain their digests, paths, decisions, rationale and accountable reviews. Licensed photos
are morphology-only with every photo delivery purpose denied. Generated images are disclosed and
normal-UI-only; public-snapshot artwork and raster export remain denied.

G has its own validator and tests through `node scripts/dog-portrait-cohort.mjs --cohort=g --check`.
The explicit profile loader, generated prompt-template allowlist and reviewer batch list include G01.
Both runtime consumers use matching versioned compiled data. Previous published artwork and full
profiles are protected by exact record and byte comparisons.

## Lessons to carry forward

- Compare proposed identities with published canonical names, aliases and registry standards before
  acquiring references or generating. Distinct catalog IDs alone do not prove distinct breed identities.
  Legitimate color/size varieties remain eligible, but duplicate synonyms do not count as new coverage.
- Exact source descriptions matter more than filenames or search results. Visually inspect the original,
  preserve the File revision and original chain, and resolve public-domain policy metadata before a call.
- Distinguish natural bobtails and standard-permitted short tails from cropped framing. Correct a tail
  that violates the specific breed standard; do not regenerate a good native for speculative concerns.
- Prompt file bytes and submitted prompt text can differ by a final newline. Hash the actual submitted
  string, store the file hash separately, and preserve correction evidence instead of rewriting history.
- Shared morphology references need identity-specific asset namespaces when a legitimate variety uses
  the same source bytes as a published parent. Do not overwrite existing rights rows.
- Separate source/profile/native acceptance from integration and publication. A good image can still
  be held for duplicate identity or unresolved rights; it never counts toward completed distinct pairs.

## Publication receipt

Pending final integration and production verification.
