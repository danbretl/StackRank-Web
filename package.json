{
  "type": "module",
  "scripts": {
    "check": "node --check app.js",
    "check:functions": "deno check --no-lock supabase/functions/*/index.ts",
    "test": "node scripts/run-tests-with-report.mjs",
    "test:watch": "node --test --watch \"tests/**/*.test.js\"",
    "verify": "npm test && npm run check && npm run check:functions",
    "author:packs": "node scripts/author-suggestion-packs.mjs",
    "screenshots": "node scripts/capture-screenshots.cjs"
  }
}
